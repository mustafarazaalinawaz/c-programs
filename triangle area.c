#include <stdio.h>
#define PI 3.14159

int main() {
    float  base, height;

    

    
    // Area of Triangle
    printf("Enter the base and height of the triangle: ");
    scanf("%f %f", &base, &height);
    printf("Area of Triangle = %.2f\n", 0.5 * base * height);

    return 0;
}
