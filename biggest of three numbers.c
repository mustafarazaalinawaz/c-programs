#include <stdio.h>
int main() {
int a, b, c, big;
printf("Enter three numbers:");
scanf("%d %d %d", &a, &b, &c);
}
big = (a > b)?((ac) ?a c) ((bc) ? b: c);
printf("The biggest number is: %d\n", big);
return 0;
}