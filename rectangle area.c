#include <stdio.h>


int main() {
    float length, width;

   

    // Area of Rectangle
    printf("Enter the length and width of the rectangle: ");
    scanf("%f %f", &length, &width);
    printf("Area of Rectangle = %.2f\n", length * width);

    
    return 0;
} 
